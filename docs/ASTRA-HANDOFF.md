# アストラ引き継ぎ書 — UNGR ARCHIVE / ムー君の冒険

> **コンセプト・ゴールの正本：[`docs/CONCEPT.md`](CONCEPT.md) と本書。**
> 2026-09-07にオーナーが明示した内容をCONCEPT.mdへ保存。以下の過去の実装記録が食い違う場合、最新のオーナー宣言を優先する。
>
> **核**：UNGR ARCHIVEが顔。プレイヤーは人間、ムー君は言語化できない思考を通す媒体。NOUと殻ライフを混ぜて壊さない。
> **人間ゲージ**：行動で上がり、しないと下がる。祝福でも警告でもある。
> **表エンド**：人間100% → 鏡で自分と対峙 → 一度負ける → 責任を持って勝つ。拒否ならハード再スタート。
> **裏エンド**：深く自己分析できたら成功。クリア必須ではない。
> **戦闘・関係性**：怒哀=ACTION／喜楽=RPG。友達のその後は、同行・殻・現地など相手によって異なる。
> 入口は常にホーム。機能を増やしすぎない。3D試作の祠・体力増加や既存章の人間60%を最終ゴールと扱わない。3D側の喜エリアACTION・一律同行は正本と不一致であり、今後の整合対象。

> 作成目的: 制作をアストラに引き継ぐための**思想・構想・技術・運用・優先順位**の一本化。  
> リポジトリ: `ekata1214/nois-mou-adventure`  
> 本ドキュメント更新時点の `main`: ホーム先行入口・ポケモン風RPG・十字キー修正・バグ掃討まで反映済み。

---

## 0. まず開くもの

| 用途 | URL |
|------|-----|
| **ユーザー向け入口（ホーム）** | https://ekata1214.github.io/nois-mou-adventure/ |
| ゲーム本体 | https://ekata1214.github.io/nois-mou-adventure/game.html |
| 共有用（QR等） | https://ekata1214.github.io/nois-mou-adventure/mobile.html |
| GitHub | https://github.com/ekata1214/nois-mou-adventure |

**ルール:** 人に渡すリンク・毎回の確認リンクは、原則**ホーム**を出す。ゲーム直リンクは「遊ぶときだけ」。

---

## 1. これは何のプロダクトか

### 一言
**UNGR ARCHIVE（アングラアーカイブ）** という「脳の中の議論をする場」の中に、体験プロトタイプとして **ムー君の冒険（Nois Mou）** が入っている。

- チャンネル / ブランドが本体の顔
- ゲームはフックであり、アーカイブの一部
- 「弱いSNS」や「機能フルアプリ」を目指していない

### ブランドの核（`data/archive.json`）
- 名前: UNGR ARCHIVE / アングラアーカイブ
- ハンドル: `@ungr.archive`
- タグライン: **脳の中の議論をする場**
- ステートメント: 議論が華。なぜ好きか / なぜ嫌いかを議論する
- キーワード: アングラ・批評・表現・倫理・思想
- 役割: 世界観設計 / 映像 / CG / ディレクション

### ムー君の世界観（ゲーム側）
- ムー君は**人間の思考の中でしか生きられない**
- **殻（INTROVERT）** = 内省・言葉・生活・回復
- **NOU（EXTROVERT）** = 外のフィールド・遭遇・採集・緊張
- VOID に触れすぎると存在を保てない（ゲームオーバー文言にも反映）
- 人間メーター / 闇の思念体 / 体力が中核リソース

### 二本柱（絶対に混ぜて壊さない）
1. **NOU** — 移動・遭遇・選択・緊張・祝福と警告
2. **殻ライフ** — ゆっくり・触れる・見せびらかさない温かさ（競争・暴力以外の遊び）

詳細構想: `docs/shell-life.md`

---

## 2. 作者（オーナー）の思想・判断基準

アストラが迷ったら、ここに戻る。

### プロダクト判断
- **意味が薄い機能は足さない。** 作者本人が「これ意味ある？」と感じる瞬間がある。その感覚は正しいセンサー。
- 足すなら核を強化するものだけ:
  - アーカイブとしての「視点の痕跡」
  - ムー君体験の手触り
- **チャット／ユーザー同士の本格コミュニケーションは今はやらない。** 需要はあっても、運用・荒らし・空の広場問題が本体を壊す。外の X / Discord に逃がす想定。
- **感想・批評の軽い置き場**は将来相性が良い（映画・美術の1〜3行など）。ただし「弱いSNS化」は避ける。最初は作者視点＋超短い反応まで。
- ゲームはフック。ブランドをゲームタイトルだけで上書きしない。

### UI / デザイン判断（フロント作業時）
- 既存の UNGR / Netflix風ハブの言語を壊さない（赤 `#e50914`、暗背景、ロゴは **オリジナル** `assets/ungr-avatar.jpg`）
- AI生成の脳マーク等は採用済みで却下済み。**オリジナルアバター以外のマークを勝手に足さない**
- ハブの第一画面は「アーカイブのホーム」。ゲームは明示PLAYで入る
- ゲーム画面は携帯の強制横画面（`force-landscape`）を前提に検証する
- 過剰なカードUI・紫グラデ・クリーム＋テラコッタ定番AI見た目は避ける（既存ルールがある場合はそれに従う）

### 開発スタイル
- 小さく直して `main` に載せて Pages で確認
- キャッシュバスト `?v=日付+タグ` を忘れない（特に携帯）
- LFS必須アセット（部屋・ムーGLB）を壊さない。Pagesは Actions 経由（`docs/pages-glb.md`）
- 推測で世界観を薄めない。不明なら「試作」「未実装」と明示

---

## 3. 現在の体験フロー（ユーザー視点）

1. ホーム（`index.html`）を開く → UNGR ARCHIVE ハブ
2. 「ムー君を遊ぶ / PLAY / 冒険」で `game.html`
3. タイトル「スタート」→ NOU フィールド
4. 十字キーで移動（携帯は**十字のみ**。キャンバスタップ移動は無効）
5. 気配に触れる → 遭遇
   - 喜・楽エリア → **RPG**（初期ポケモン風UI）
   - 怒・哀エリア → **ACTION**（リアルタイム）
6. Tab / 「殻へ戻る」→ 殻（3D部屋・質問100字・クラフト）
7. 左端スワイプ / ホームボタン → アーカイブホームへ（ブラウザ戻るに依存しない）

章進行（`js/story.js`）:
0 殻で100字 → 1 遭遇3回 → 2 採集5 → 3 クラフト1 → 4 使う1回 → 5 人間60%

---

## 4. リポジトリ地図（アストラが触る場所）

### エントリ
| ファイル | 役割 |
|----------|------|
| `index.html` + `js/app.js` + `css/app.css` | アーカイブハブ |
| `game.html` + `js/main.js` + `css/style.css` | ゲーム |
| `css/game-back.css` | ホーム戻りUI |
| `mobile.html` | 共有・QR（ホーム優先） |
| `data/archive.json` | ブランド・動画・お知らせ |
| `manifest.webmanifest` | PWA（`start_url` = index） |

### ゲーム中核
| 領域 | 主なファイル |
|------|----------------|
| 進行・魂 | `js/soul.js`, `js/story.js` |
| ワールド | `js/world.js`, `js/field-art.js`, `js/props.js` |
| エンティティ / 遭遇 | `js/entities.js`, `js/encounter-transition.js`, `js/battle-field.js` |
| RPG UI | `game.html` `#encounter-screen`, `css/style.css` `.rpg-*` |
| ACTION | `js/combat-action.js`, `js/enemy-patterns.js`, `js/difficulty.js` |
| 殻3D | `js/shell-room.js`, `js/shell-muu-3d.js`, `js/muu-gltf-loader.js`, `js/shell-*.js` |
| 採集クラフト | `js/gatherables.js`, `js/gather-craft.js`, `js/craft-effects.js` |
| 音 | `js/bgm.js`, `js/audio.js` |
| 携帯操作 | `js/mobile-controls.js`, `js/dpad-input.js`, `js/mobile-viewport.js` |
| スワイプ戻る | `js/swipe-back.js` |

### アセット（重要）
- 2Dムー: `assets/muu/{front,back,left,right}.png`
- 3Dムー: `assets/muu/*.glb` + `manifest.json`（希望: `speak-mou5.glb` / `good-mou.glb`）
- 部屋: `assets/room/this ver2.glb` + `manifest.json`
- アイコン: `assets/icons/{hylics,giger}/`
- 景色: `assets/scenery/`
- BGM/VO: `assets/bgm/`, `assets/vo/`
- ブランド: `assets/ungr-avatar.jpg`, `assets/title.png`

**注意:** GLBは Git LFS。ローカルで `git lfs pull` しないとポインタのまま。Pagesは workflow で LFS checkout。

---

## 5. すでに `main` に入っている主な成果（重複作業しない）

| 内容 | メモ |
|------|------|
| ハブ先行入口 | リンクはホーム。ヒーロー全体タップでゲーム直行しない |
| Netflix風ハブ | ホーム / 動画 / 冒険 / 情報 |
| オリジナルロゴ | AI脳マーク不採用 |
| アプリ内ホーム戻り | スワイプ＋ボタン → `index.html` |
| 携帯長押し抑制 | テキスト選択・コールアウト対策 |
| タイトルUI | スタート / ホームに戻る |
| ゲームオーバーループ修正 | 復活HP・遭遇ハードクリア |
| 移動は十字のみ | タップ移動オフ |
| RPGを初期ポケモン風 | 左右斜め立ち・HP・右下コマンド |
| 十字ヒット修正 | **指の下のボタン判定**（回転AABBアナログ廃止） |
| バグ掃討 | 戦闘中誤VOID、逃走二重連敗、難易度二重、BGM無音、ランプ回復、文字数緩和など |

直近キャッシュ例: `?v=20260730dpadbtn`

---

## 6. 既知の注意点・壊れやすいところ

1. **強制横画面 (`force-landscape`)**  
   `#app` を CSS 回転。`vw/vh`・`position:fixed`・safe-area・タッチ座標がズレやすい。UIは `#app` 基準・`%` / container / `--app-w/h` を優先。

2. **十字キー**  
   アナログ中心計算は失敗済み。現状は `elementsFromPoint` でボタン直下。挙動が変なら**見た目ボタンとの一致**を最初に疑う。

3. **戦闘中の座標**  
   バトルフィールド座標をワールドVOID判定に使うとHPが溶ける。`fieldVoidProbe()` / overworld snapshot を維持。

4. **BGMクロスフェード**  
   プレイヤーごとにフェードタイマー。共有1本に戻すと無音バグ再発。

5. **殻GLB欠落**  
   空の `roomRoot` で NaN 配置しない（空ボックスはフォールバック）。

6. **キャッシュ**  
   `game.html` の `main.js?v=` と、`main.js` 内の `mobile-controls.js?v=` 等を揃える。片方だけ古いと「直ってない」に見える。

7. **ハブCSSキャッシュ**  
   `index.html` は `?v=20260729swipe` など。ハブ変更時もバストする。

---

## 7. 殻ライフ構想（未完〜一部実装）

`docs/shell-life.md` が正。要約:

| Phase | 内容 | 状態イメージ |
|-------|------|----------------|
| Life 0 | 殻タップ移動 | 未完寄り |
| Life 1 | メモの壁 | 一部（クラフト memo_wall） |
| Life 2 | 採集＋簡易クラフト | **実装済み寄り**（lamp / pot / memo） |
| Life 3 | 庭 | 未実装 |
| Life 4 | ペット | 未実装 |
| Life 5 | 別荘 | 未実装 |

方針: NOUだけ強くせず、**拠点が育つ**。ゼルダの「本拠が変わる喜び」を、暴力以外でやる。

---

## 8. 次にやると良いこと（優先候補）

アストラは全部やらなくていい。オーナーとすり合わせつつ、上から。

### A. 体験の安定（今すぐ価値）
- 実機（iPhone縦持ち強制横）で十字・遭遇・殻・ホーム戻りを通しプレイ
- 残る操作違和感・レイアウト切れの個別修正
- LFS / Pages で3Dが本番でも出るか確認

### B. 核の強化（意味がある方向）
- 殻ライフ Life 0（タップで寄る）またはメモ壁の手触り向上
- RPG/ACTIONの手触り差をもう少し明確に（今は様式は分かれている）
- 章ヒント・チュートリアルの分かりやすさ

### C. アーカイブ側（慎重に）
- 作者の短い感想／作品メモを置ける最小UI（SNS化しない）
- `archive.json` の更新運用を簡単に

### D. やらない／後回し
- アプリ内チャット・フォロー・タイムライン本格実装
- Store申請前提の大改装（まずWeb/PWA）
- 機能の横展開ラッシュ

---

## 9. 開発・デプロイ手順（最短）

```bash
git lfs pull
# ローカル
./setup-and-run.sh   # or serve.py / start-local.sh → :8765

# 変更後
# ブランチ cursor/<name>-5547 → PR → main マージ
# GitHub Actions「Deploy GitHub Pages」成功を待つ
# ハードリロードで確認
```

- ブランチ名慣例（クラウド作業）: `cursor/<descriptive-name>-5547`
- base: `main`
- ユーザーへの確認リンク: **ホーム** https://ekata1214.github.io/nois-mou-adventure/

テスト補助:
- `node scripts/test-dpad-input.mjs`
- `node scripts/test-bug-sweep.mjs`

---

## 10. セーブデータ

- キー: `localStorage` `nois-mou-soul-v1`（`js/soul.js`）
- 中身: HP、人間、闇、feeds、inventory、crafted、章、勝敗ストリーク等
- 破壊的変更時はキーバージョンかマイグレーションを意識

---

## 11. トーン＆ボイス（文章を足すとき）

- 説教くどくしない。短い。余白がある
- アングラだが中二病コピペにしない
- 殻は優しいが甘くない（「ごまかせない」）
- NOUは選択の重さ（殺す／食べる／無視／友達）
- UIラベルは日本語メイン、ブランド英語の添い（ENCOUNTER 等）は既存踏襲

---

## 12. 連絡・参照

- メール: ungr.archive@gmail.com
- YouTube / TikTok / IG: @ungr.archive
- 既存ドキュメント: `docs/shell-life.md`, `docs/pages-glb.md`
- 本引き継ぎ: `docs/ASTRA-HANDOFF.md`（これ）

---

## 13. アストラへの短い依頼文（コピペ用）

```
UNGR ARCHIVE / ムー君の冒険を引き継いで。
正本は docs/ASTRA-HANDOFF.md と docs/shell-life.md。
入口リンクは常にホーム https://ekata1214.github.io/nois-mou-adventure/
ゲームはフック、アーカイブが顔。機能は増やしすぎない。
殻ライフと NOU の二本柱を壊さない。
強制横画面・十字キー・LFS/Pages キャッシュに注意。
まず実機通しプレイで残バグを見てから、殻ライフか手触り改善へ。
```

---

*このファイルは制作引き継ぎ用。実装の詳細がコードと食い違う場合はコードと `main` の履歴を優先し、この文書を更新すること。*

## 14. 2026-09-07 オーナー確認・3D探索初版

### 今回ユーザー本人に確認した完成像
- 画質より、ゼルダのようなオープンワールド感を優先する。
- 自由な移動／発見と寄り道／世界への働きかけ／採集から殻の生活、の四つが目標。
- 脳内・夢の世界。喜怒哀楽で景色や法則が異なる。
- 視点はムー君の背後から見る3D。スマホとPC両対応。
- 歩く・走る・ジャンプを土台に、浮かぶ言葉・記憶へ「思考から思考へ飛び移る」。
- 探索先には住人、記憶の風景、仕掛け、素材や生き物。
- 友達になる設定は新提案ではなく既存の正典。友達のその後は、同行／殻へ来る／現地で関係が続く、相手により異なる。
- ホームのNetflix風アーカイブも洗練させる。入口は引き続きホーム。

### 今回実装した範囲
- ホーム：既存オリジナルアバターを使うブランドの第一画面、余白・文字階層・動画一覧・スマホ表示・作品への導線を整理。
- ホームから `explore.html` に入る **3D探索の小規模な試作**。既存 `game.html` はそのまま遊べる。
- `js/explore.js` / `js/explore-state.js` / `css/explore.css`：背後カメラ、キーボード・タッチ操作、走る・跳ぶ、四感情の地形、言葉の足場への移動。
- 24個の記憶のかけら、4体の気配、同行／殻／現地の関係選択。今回は各気配に同じ選択肢を実装。相手固有の条件や会話分岐は今後。
- 殻の試作はダイアログの棚・灯りクラフト・メモ壁。3Dの生活空間、庭、ペットはこの試作では未実装。
- 専用保存キー `nois-mou-explore-v1`。従来の `nois-mou-soul-v1` に書き込まない。今回の採集は既存ゲームの持ち物と未連携。
- ムー君は既存 `speak-mou.glb` を背景読込。待機中・失敗時は既存ムー君画像で操作可能。新規モデルやAIブランドマークは追加していない。
- ブラウザの戻るでホームが暗転したままになる問題、ストレージ制限時にゲームへ遷移できない問題も対処。

### 確認と限界
- `node scripts/test-explore.mjs`：保存復元、重複資源、クラフト、友達、四地域、思考移動の範囲。
- 既存十字キーとバグ掃討のテストを実行。バグ掃討テストの旧座標回転期待値を現行のボタン直下方式に合わせて修正（本体の十字キーは変更していない）。
- 実Three.jsのシーン・移動・着地・会話・採集・殻の流れを、描画とDOMを代替した実行シミュレーションで確認。
- HTMLのローカルリンク・重複ID、GLBの実バイナリを確認。
- **ブラウザでの描画QAとスマホ実機検証は未実施。** 特に3Dモデルの見た目、端末負荷、カメラ操作は次に優先確認。
- ゼルダ規模の完成ではない。登攀・泳ぐ・広域ワールド・本格物理パズル・3D戦闘・既存章進行との統合は今後。

## 15. 2026-09-07 ムー君の動作初版

- 3D探索は `assets/muu/mou-actions.glb`（約10MB）を使用。既存 `speak-mou.glb` の骨格・メッシュ・保持できるテクスチャを引き継いだ派生素材。外付けSSD/HDDの原本は変更していない。
- 待機、歩行、走行、ジャンプ、落下、着地、手振り、拾う、思考移動の9クリップ。`js/mou-motion.js` が移動優先・短い補間で切り替え、会話中は停止する。
- R または右下の「手を振る」。拾う動きは記憶の採集時。移動すると身振りを終了する。既存の移動・ジャンプ・思考移動の操作は同じ。
- 編集用は `assets/muu/actions/mou-actions.blend`。Blender 4.4.3で `scripts/blender/build-mou-actions.py` を実行すると元GLBから再生成できる。9動作はアクションとして保存。従来の会話モデル自動書出しとは分離。
- 元素材の参照切れ画像ノードのみ除去して出力。動作は骨回転による初版で、足の接地IK、坂への足合わせ、戦闘、泳ぎ、登攀は未対応。
- 確認：Blenderで待機・歩行・走行・手振りを静止画確認。実Three.js r170で状態切替・中断・一時停止・連続切替をテスト。`node scripts/test-mou-motion.mjs /path/to/three.module.mjs`。既存3テストと描画を代替した探索実行テストも合格。
- 今回のブラウザ描画・スマホ実機での動作確認は未実施。Blender確認はソリッド表示で、ゲーム内マテリアルの見え方までは保証しない。

## 16. 2026-09-07 一つの野原の品質改善

- オーナー希望は「一つでよいからオープンワールドの品質を上げる。PS3くらいの質感」。今回の集中対象は「喜／ほどける野原」。PS3作品相当の完成度を達成したという意味ではなく、ブラウザ上の初版を磨く段階。
- `js/meadow-world.js`：曲がる土の道、水辺、木陰、丘の石造アーチ、遠景の山、葉の輪郭を持つ立体的な木、風で揺れる草、動的な影、細かい地面・石材、水面の波紋、漂う粒子。地形・植生は決まった種の手続き生成。外部素材・追加画像ダウンロードはない。
- 出発点を喜の入口へ移し、見えている遺跡へ道をたどれる構成。三つの石碑に E／「耳をすます」で触れ、短い風景の記憶を読む。今回の文章は試作として新規追加。
- 保存に `discoveries`（water/grove/arch）を追加。古い探索セーブは空配列として復元し、採集・友達・灯り・メモは継承する。従来ゲームのセーブは別。
- 木の幹と大きな岩への簡易当たり判定、地形・障害物の手前で短くなるカメラ、歩行4／走行8に調整。物理エンジンや足の接地IKは未導入。池は浅い地形で、泳ぐ操作はない。
- タッチ端末は影1024px・草の表示数55%、PCは影2048px。実端末に合わせた継続的な負荷調整は今後。
- 確認：ローカルの実ブラウザで1280幅・390幅の描画、開始と足場への移動、描画エラーログなしを確認。390幅は表示テストで、スマホ実機の性能・タッチ検証ではない。
- 自動確認：既存の探索／十字キー／バグ掃討／ムー動作テスト。新規 `scripts/test-meadow-runtime.mjs /path/to/three.module.mjs` は実Three.jsの地形生成、移動・ジャンプ・足場・3か所の発見・重複防止・採集・友達・灯りを確認（DOMと描画は代替）。

## 17. 2026-09-07 肌・脳の色と微動、切り株の接地

- オーナー指定：肌色の体、少しグロテスクな赤黒い脳。脳は常にごく微細にうごめく。切り株に乗れること、挙動の違和感を修正、草原はよりビビッドな緑。
- 前回の生成処理の不備：Blenderの遅延読込画像は `has_data=False` でも欠落ではない。これを欠落と判定して画像ノードを除去したため灰色になっていた。packed imageのピクセルを読み込んでから判定するよう修正。元GLBの肌Base Color自体もマスク画像に接続されており、肌は指定色の材質で再設定した。
- SSDの `ムー君の冒険/brain-baked.png` をコピーして `assets/muu/brain-baked.png` に保存。脳はこの既存画像を使用し、ゲーム側で赤黒く色調整。目・歯の元の画像は保持。GLBは約28MB。元SSD/HDD素材は未変更。
- 調べた `speak-mou-super-fix-base.blend` の脳にはArmatureのみでGeometry Nodesは残っていなかった。`js/mou-appearance.js` がスキニング前に法線方向へ最大約0.003モデル単位のゆっくりした変形を加えて代用。影も同じ変形。これは元Geoの復元ではなくブラウザ側の代替で、Blenderファイルにはこの微動は含まない。
- 道沿いに4つの木の切り株。木目の上面にジャンプ着地できる。既存の3つの石碑の台にも乗れる。横面の衝突と上面の支持を分離。歩いて縁を出ると落下する。
- 下り坂で毎フレーム接地を失い落下／着地が繰り返されていた問題を修正。小さい段差・坂を歩く間は接地を維持し、ジャンプ中や高い足場から離れた時は重力を適用する。衝突で止まった時は意図した移動量ではなく実移動量でアニメーションを選択。
- 草・地面・木の色の彩度を上げた。
- 確認：実ブラウザで肌・脳・緑・切り株の描画、ジャンプ、シェーダーエラーなし。自動テストでGLBの脳画像と肌色、微動の影／本体の時刻共有、脳以外が変形しないこと、切り株着地と再ジャンプ、側面衝突、連続した坂の接地を追加。従来テストも合格。スマホ実機・全地形の手動走破は未実施。

## 18. 2026-09-07 体型・全動作と、水辺の物理

- オーナーは脳テクスチャ・モデルを公開GitHubへ送信することを明示承認。前節の未公開修正も今回まとめて公開する。
- 全身メッシュとバインド骨格を同じ変形で調整。肩幅を広げ、腰幅と胸・臀部の突出を抑えた。全9クリップの基準姿勢を少し広い足幅と腕の位置へ変更し、歩行・走行の体幹の揺れを抑制。単一の待機ポーズだけの変更ではない。
- 池の以前の円盤とガウス状の池底は縁で高さが一致せず、浮いて見えていた。`POND` に中心・半径8.8・水位-1.6・深さ1.25を集約。岸360方向で池底が水位へつながる形状へ変更。浅瀬として池底を歩く。
- `js/field-physics.js`：最大1/120秒刻みのキャラクター物理。加速／減速、空中制御、重力、床・側面・頭上の衝突、壁に沿う速度、切り株支持、浅瀬の抵抗・水没割合に応じた部分的な浮力。15–120fpsの試験で壁をすり抜けないことを確認。
- 思考移動の軌道も0.1単位以下で障害物を確認し、行く手が塞がると中断して落下へ。同行する気配にも障害物の横方向の解消処理を追加。
- `js/pond-water.js`：入水・水中歩行・出水の位置と速度に応じて波紋を発生。水面法線の揺れと拡大する輪を同期。岸を越えた輪は描画しない。
- これはゲーム向けの近似的なキャラクター物理。岩・木・地形は静的。剛体を押す／転がす、動的な浮遊物、流体の質量保存・水流、泳ぎ・潜水の完成を意味しない。
- 確認：`scripts/test-field-physics.mjs` で岸360方向・フレーム差・高速衝突・頭上・切り株・水抵抗と池底。既存探索実行テストに入水／移動／出水の波紋発生と静止時の重複防止を追加。既存全テストも実行。
- 実ブラウザで新しい後ろ姿・岸の接続、検証用ページから実際の物理処理で入水・水中歩行を確認。`tools/physics-preview.*` はローカル検証専用でGit・Pagesから除外。

## 19. 2026-09-07 野原の遭遇・戦闘

- 喜の野原に既存の「怒りの残火」「嫉妬の棘」「孤独の影」を配置。既存giger/eyeball画像を顔に、立体の金属殻・釘・四脚を付けた派生デザイン。ゼルダの素材は使用しない。
- F／思考を振る、C／回避、T／注目。2種類の交互攻撃、スタミナ回復、回避の短い無敵時間、体力5。右腕・前腕・体幹へのランタイム加算姿勢と光の弧。GLBの9クリップは変更していない。回避はステップで、回転回避・武器装備は未実装。
- 敵は巡回→発見→追跡→予兆→方向固定の突進→回復の隙。既存パターンから突進型・横移動型を使用。地形と障害物への移動衝突、壁越し攻撃の遮断、距離による帰還。敵同士の剛体衝突・障害物を迂回する経路探索は未実装。
- 戦闘後は消滅せず静まる。Eで友達として同行／待機。encountersにcalmed/friendのみ保存し、旧セーブを維持。倒れた時は入口で体力を戻し、採集・友達の進行を保持。戦闘は探索進行の必須条件にしない。
- scripts/test-field-encounters.mjs: 予兆の方向固定・一度だけの命中・回避無敵・回復の隙・保存。test-meadow-runtimeに注目、命中、停止、静まる、友達、再開を追加。既存探索・水辺物理・動作・十字キー・バグ掃討テストを実行。
- 実ブラウザでモデル読込、敵の姿・接近・ダメージ・敗北画面を確認。tools/combat-preview.*はローカル検証専用で公開対象外。スマホ実機の性能確認は未実施。
- オーナーは利用上限に達した時の無料リセット使用を承認。今回の作業中は上限前のリセットは行わない。

## 20. 2026-09-07 スマホ操作・自然な岩

- スマホのWeb版に、指を滑らせて斜め移動できるパッド、独立した視点ドラッグ、親指で押せる攻撃・回避・注目ボタンを配置。縦横画面とsafe-areaに対応。指を離す／中断時に移動解除。タッチ端末の描画密度を最大1.25へ抑制。ネイティブアプリ配布ではない。
- 岩は5種の不規則な形を共有し、広い割れ目・鉱物の粒・色むら・上面の苔を手続き生成。256pxの共通画像と形状を再利用し、大きな素材ダウンロードは追加しない。
- 確認：390×844の実ブラウザ表示、探索・戦闘・物理の既存テスト、パッドの斜め入力／中央停止／キャンセル解除。スマホ実機のマルチタッチとフレームレート測定は未実施。

## 21. 2026-09-07 探索を一周できる冒険・全体改善

- オーナー要望：不具合を洗い、造形・空・ゼルダを参考にした遊びを広く改善。40〜50代でも触れる、複雑にしない設計。今回の軸は「宝箱で能力を得る → 休憩・採集 → 丘の祠 → 祝福を持ち帰る」。既存の採集・友達・殻ライフは維持。
- `js/adventure-state.js` / `js/field-adventure.js`：地図と目的地案内、3つの宝箱、6か所の木の実、3か所の焚き火・発見済み拠点への移動、木の実2個で料理、料理で体力3回復、葉→雫→光の簡単な祠、祠の宝箱で最大体力6。休憩で体力・スタミナ回復、採集ポイント再生。通常は近づいて同じ「触れる」ボタンで操作。
- 風の宝箱を開けると、空中で再度「跳ぶ」を押して滑空。落下速度上限2.2、移動5、スタミナ毎秒9消費、着地・入水・枯渇で解除。もう一度押すと布をたたむ。布の立体表示はランタイム生成でGLB変更なし。
- 保存に `adventure` を追加し古いセーブを補完。宝箱・灯り・料理・拠点・設定を保持。保存した休憩所から再開／敗北復帰。料理を食べる際に所持数・体力を再検証し二重取得を防止。殻と旧ゲームの保存は分離を維持。
- 操作を簡略化：近くの正面の敵への照準補助、初期設定は敵を20%遅くする／被弾後の猶予を長くする。「戦わずに探索」も地図から選択可。画面の方角を押すと視点を戻せる。地図は閉じるボタンを上にも置き、目的地は小さな印だけでなく大きな選択欄から選べる。
- 不具合修正：友達の敵を待機させても友情を失わない `friend-stay`、操作フォーカス喪失時の一時停止、遠隔／壁越しの古い「触れる」対象の再検証、低い歩行足場への乗り上げ、箱状障害物を含むカメラ遮蔽、遺跡の柱・上部と岩の高さの当たり判定、敵の局所的な障害物回避・プレイヤーとの重なり軽減、体力増加後の回復・復帰上限維持、移動時の残留攻撃解除、横画面でのパッド下端切れ、保存失敗の通知。
- 見た目：既存の目の絵を保持しつつ立体の眼球・眼窩・顎・角・背甲を追加。怒り／嫉妬／孤独で輪郭を変え、予兆を地面に沿わせる。木に立体の樹冠、花の群落、支柱が池底につながる桟橋、薄い岩状の思考足場。大きな足場の下側が頭・視界を隠す箇所も縮小し、風の宝箱を脇へ移した。
- `js/field-sky.js`：継ぎ目のない雲画像を起動時に一度生成し空へ使用、ゆっくり回転。追加の外部画像・ダウンロードなし。夜や天候システムを実装した意味ではない。
- テスト：従来6系統＋adventure／探索実行を拡張。保存の破損値、重複取得、祠の開錠、料理の消費、15–120Hzの滑空、実際の近接操作・遠隔拒否、祠の体力増加、料理・休憩所復帰、一時停止、待機中の友情を確認。PR時に再実行する `.github/workflows/field-checks.yml` を追加。
- 実ブラウザ：宝箱→灯り3つ→祠の祝福、雲・造形、390×844の縦画面、844×390の横画面、地図の開閉、描画エラーログなし。検証専用の `tools/adventure-preview.*` は公開除外。スマホ実機の速度／マルチタッチ、全地形の完全な走破は未確認。
- 範囲の限界：すべての不具合の不存在やゼルダ全作品の機能網羅を保証しない。自由登攀、弓・盾の装備、乗り物、深い水での泳ぎ、大型ダンジョン、昼夜・天候、動的剛体パズルは今後。敵の経路探索は近傍の回避で、完全なナビメッシュではない。無理に操作やアイテム管理を増やさず、まず一つの野原の一周を整えた。

## 2026-09-08：UAの殻ライフを3D試作へ

- `explore.html` の「殻へ帰る」で、同じムー君を3Dの部屋へ移す。NOUの戦闘・移動は停止し、戻ると元の位置・向きを復元する。
- 原本 `assets/room/this ver2.glb` は変更せず、家具だけを `shell-lite.glb`（約13.5MB）へ軽量化。見渡せる切り開いた部屋で使用。読み込み失敗時は簡易家具。
- 殻の6か所でかけら採集（各5分で再出現）→3つで言葉の器→自分で書いた500字までのメモ。60個まで保持、空欄・キャンセルでは器を消費しない。メモの一部を部屋の壁にも表示。保存先は既存の試作セーブ内 `shellLife`。旧メモは保持。
- 自律散歩と「一緒に過ごす」で落ち着く時間を実装。高度なたまごっちの入口となる小さな試作であり、食事・育成・長期の性格変化の完成版ではない。
- 既存の `drone.mp3`（NOU）／`heal_long1.mp3`（殻）を「音：入」で再生。最初は無音、切替時に前曲を止め、非表示・一時停止時も停止。
- UAの灰肉色の空、赤い天体、黒紫の樹木、灰色と錆色の草原へ変更。歩ける道の視認性は維持。敵は独自の肋骨・長い外殻・管の造形を追加。
- 怒・哀のACTION敵を各感情の領域へ移動。喜の敵は選択式の遭遇に変更し、3回耳をすますか、思考をぶつけて緊張を解ける。楽にはまだ敵を置いていない。
- 人間ゲージ・VOID・表裏エンド・相手ごとの全友達関係は未統合。機能を増やす際も `CONCEPT.md` を優先する。
- 殻の保存・変換・復元・戦闘停止・RPG対話・音切替を回帰テストへ追加。ブラウザPC／390px幅で実表示を確認。実機スマホのGPU・音声経路は別途確認が必要。

## 2026-09-08：殻の壁と外の宇宙は原作を維持

オーナー指定：「からの中の壁とか外が宇宙になってるのは踏襲して」。家具だけの赤黒い箱への置換は撤回。`shell-lite.glb` は原本の壁・床・天井・照明・家具を含む軽量版へ更新し、壁は家具より高い形状精度で残す。元の殻と同じ星空の演出・照明設定を使用。採集・器・メモはその中に維持する。今後もUAへの調整を理由に、既存の殻の建築と宇宙の世界観を勝手に作り替えない。
